;(() => {
    'use strict';
    let arr = ['javascript', 'php', 'c', 'c++', 'java', 'python', 'ruby', 'c#', 'swift', 'objective-c'];
    let inArray = (element, array) => {
    return element === array.some()
    }
})();