    'use strict';

    function pushButton() {
        let count = 0;
        let push = document.getElementsByClassName("button");
            if (document.onclick = push) {
                count ++;
                console.log(count);
            }
    }


