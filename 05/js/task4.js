'use strict';

let num1 = parseFloat(prompt('Insert first number:', ''));
let num2 = parseFloat(prompt('Insert second number:', ''));

if (num1 > num2) {
    console.log(num1);
} else {
    console.log(num2);
}
