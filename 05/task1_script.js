'use strict';

let a = prompt ( "Введите значение 'a'" );
let b = prompt ( "Введите значение 'b'" );

let x;
if ( a = b ) {
    x = 400;
} else if ( a > b ) {
    x = ( a + b ) / ( 2 * 4 );
} else if ( a < b ) {
    x = ( a - b ) + ( 2 / ( b * 4));
}
console.log( x );